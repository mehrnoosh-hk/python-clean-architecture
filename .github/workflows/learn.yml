name: learn-github-actions
on: 
  [push]
jobs:
  check-version:
    runs-on: ubuntu-latest
    steps:
      - name: check-out-code
      - uses: actions/checkout@v3
      - name: hello-world
      - run: echo "Hello World!"
